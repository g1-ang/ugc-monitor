#!/usr/bin/env bash
# 더블클릭으로 실행 가능한 wrapper (start.sh 실행 + 터미널 창 유지)
cd "$(dirname "$0")"
./start.sh
echo
echo "════════════════════════════════════════════════"
echo "  서버가 종료되었습니다. 창을 닫으려면 Cmd+W."
echo "════════════════════════════════════════════════"
