# 2026-06-05 작업 재개 노트 (집 맥북에서 이어서)

## 어제 (06-04) 한 일 요약

### 🔴 결정적 발견: Gemini 2.0 Flash 6/1 deprecated
- NAMC Vertex AI 에서 `gemini-2.0-flash` 가 6/1 부터 종료됨
- 호출이 silent fail (404) → 응답 본문 비어있고 코드는 None 반환 → 모든 매치 판정이 NO
- **6/1 이후 모든 스캔이 0건 매치만 받고 있었음**
- 검증: 디버그 스크립트로 NAMC 호출 → "Publisher Model `gemini-2.0-flash` was not found" 에러 직접 확인

### ✅ 적용한 수정 (모두 main 에 push 완료)

**로컬 레포** (`g1-ang/ugc-monitor-api`):
- `ugc-monitor-api/main.py`
  - `MODEL_NAME`: `gemini-2.0-flash` → **`gemini-3.1-flash-lite`** (GA, 영구 — 2.5 family 는 6/17 종료)
  - `max_tokens`: `10` → **`200`** (3.1 은 reasoning 모델, 답 전에 100+ 토큰 씀)
  - Apify 폴링 timeout 15s→30s, 3회 retry 추가 (이전 스캔 한 번 죽어서)

**snow-vibe Appwrite 레포** (`snow-vibe/prompt-ugc-monitoring`, commit `e6a4f81`):
- `functions/namc-vision-proxy/src/main.js`: 같은 fix (MODEL + max_tokens)
- `functions/scan-judge/src/main.js`: 같은 fix
- ⚠️ Appwrite 자동 재배포 여부 미확인 (GitHub Actions 없음) — Appwrite Console 에서 수동 deploy 필요할 수 있음

### ✅ 검증
- 아쿠아리움샷 (885명) 재스캔 → **18건 매치 성공** (피드 6 / 스토리 5 / 프사 7)
- @sucol_d (실제 아쿠아리움 프사 변경한 유저) 정상 매치 확인
- 19:41 스캔 후 11건 검수 완료

### ✅ 부수 정리
- `phase3_last_scan_summary.json` → `.gitignore` 추가 (매 스캔마다 더러워지는 거 방지)

---

## 현재 시스템 상태

### 로컬
- 위치: `/Users/user/Desktop/claude code/ai prompt ugc monitoring/`
- 백엔드: `ugc-monitor-api/main.py` (FastAPI, 포트 8000)
- 프론트: `index.html` (대시보드)
- 둘 다 main 에 push 완료, 최신

### 사내 페이지 (`pages.oss.navercorp.com/snow-vibe/prompt-ugc-monitoring/`)
- Appwrite Functions 가 백엔드 (15분 timeout 한계 있음)
- 코드는 fix 됐지만 Appwrite Console 에서 deploy 트리거 필요할 수 있음
- 어차피 60-80분 짜리 작업이라 timeout 문제는 별도 해결 필요
- **결정: 로컬을 메인 플랫폼으로 확정** (어제 합의)

### 모델
- 현재: `gemini-3.1-flash-lite` (영구 GA, 종료일 없음)
- 비용: ~$3/스캔 (10M input + 16K output, 3.1 flash-lite 단가)
- 속도: 885명 스캔 79분 (2.0 flash 와 비슷, reasoning 으로 살짝 느리지만 무시할 수준)

---

## 미해결 / 다음에 할 일

### 1. 슬랙 문의 보내기 (아직 안 보냄)
사내 Python FastAPI 24/7 호스팅 가능한 인프라 문의. 받을 사람은 Lee Shawn (이성우).
업데이트 버전 (시간 30→60 으로 수정 + NAMC 사내망 강조):

```
안녕하세요!
사내에 Python 백엔드 (FastAPI) 를 24시간 띄울 수 있는 인프라가 있을지 문의드려요 🙏

지금 상황:
- 마케팅팀 UGC 모니터링 도구를 만들고 있어요
- 처음엔 snow-vibe Appwrite Functions 로 시도했는데, 한 작업이 60~80분 걸려서 함수 15분 timeout 에 걸리네요
- 그래서 Python FastAPI 그대로 어딘가에 24/7 띄우는 방향으로 가려고 합니다

필요한 사양:
- Python 3.11 + FastAPI + uvicorn
- 메모리 1GB, CPU 1코어 정도면 충분해요
- 외부 API 호출 (Apify - 인스타 크롤링) + 사내 API 호출 (NAMC 게이트웨이) 둘 다 가능해야 합니다
  ← NAMC 는 사내망에서만 동작해서 (VPN 필수), 외부 클라우드는 호출 어려울 수 있음
- 한 번에 60~80분 돌아가는 작업 (timeout 없어야 함)
- 사용자 5~10명 정도가 웹 UI 로 접속해서 사용해요

질문:
1. snow corp 사내에 Docker 컨테이너 띄울 수 있는 곳이 있을까요?
   (사내 K8s, VM, Heroku 같은 PaaS, 무엇이든)
2. 만약 없으면 NCP (NAVER Cloud Platform) 가 좋은 선택일까요? NCP 에서 NAMC 호출 가능한가요?
3. 추천하시는 옵션의 신청 절차도 알려주시면 감사하겠습니다 🙏
```

### 2. Appwrite 자동 배포 확인 (낮은 우선순위)
- 사내 페이지 로컬로 옮기는 게 확정이라 굳이 안 해도 됨
- 그래도 확인하고 싶으면: Appwrite Console → namc-vision-proxy → Deployments 탭에서 어제 push (`e6a4f81`) 가 deploy 됐는지 확인

### 3. 이전에 미뤘던 작업 (이미 알고 보니 다 됨)
- ~~스캔 히스토리 최신순 정렬~~ ← 이미 됨
- ~~스캔 히스토리 행 클릭 → 매치 펼침~~ ← 이미 됨
- 추가 개선 필요 사항 있으면 그때 작업

---

## 집 맥북에서 이어받는 법

### Step 1: 코드 최신화 (저장소 둘 다)
집 맥북에 이미 clone 되어 있을 텐데, 둘 다 git pull:

```bash
# 바깥 레포 (대시보드 + index.html + .gitignore)
cd ~/Desktop/claude\ code/ai\ prompt\ ugc\ monitoring
git pull

# 안쪽 레포 (백엔드 main.py)
cd ugc-monitor-api
git pull
```

확인할 게: main.py 의 `MODEL_NAME` 이 `'gemini-3.1-flash-lite'` 인지, `max_tokens` 가 `200` 인지.

### Step 2: 서버 띄우기
```bash
cd ~/Desktop/claude\ code/ai\ prompt\ ugc\ monitoring/ugc-monitor-api
source ../venv/bin/activate  # 또는 본인 venv 경로
python -m uvicorn main:app --host 0.0.0.0 --port 8000
# --reload 빼는 게 안전 (장시간 스캔 중 코드 수정 시 죽음)
```

브라우저: `http://localhost:8000/`

### Step 3: 노트북 안 꺼지게
새 터미널 창에서:
```bash
caffeinate -i
```
(스캔 끝나면 Ctrl+C)

---

## 중요 컨텍스트 / 기억할 것

1. **NAMC URL**: `https://namc-aigw.io.naver.com/chat/completions`
   - .env 의 `NAVER_API_URL` 확인
   - 사내망에서만 접근 가능 (VPN 필수)

2. **사내 GitHub** (clone 위치): `https://oss.navercorp.com/snow-vibe/prompt-ugc-monitoring`
   - 어제 fix push 완료 (commit `e6a4f81`)

3. **로컬 백엔드 패치 적용 여부 빠른 체크**:
   ```bash
   grep -n "MODEL_NAME\|max_tokens" ~/Desktop/claude\ code/ai\ prompt\ ugc\ monitoring/ugc-monitor-api/main.py
   ```
   - `gemini-3.1-flash-lite` 보이고 `max_tokens: 200` 보이면 OK

4. **gemini-2.5-flash 도 곧 종료 (6/17)**, 3.1-flash-lite 는 영구 GA — 같은 family 라 헷갈리지 말 것

5. **매치 카운트는 스캔 끝날 때만 업데이트** (코드 line 1074-1078). 진행 중에는 항상 0으로 보임. 끝까지 기다려야 매치 카운트 정확.
